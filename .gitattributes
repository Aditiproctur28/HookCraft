# Normalize line endings to LF in the repo; check out native on each OS.
* text=auto eol=lf

# Treat known binary assets as binary (no EOL conversion, no diff noise)
*.png  binary
*.jpg  binary
*.jpeg binary
*.mp3  binary
*.mp4  binary
*.pth  binary
*.onnx binary
*.safetensors binary
